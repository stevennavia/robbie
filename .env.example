# Puerto del servidor HTTP + WebSocket
PORT=3001

# Host de escucha del servidor
HOST=localhost

# Origen permitido del cliente (CORS y WebSocket)
CLIENT_ORIGIN=http://localhost:5173

# Entorno: development | test | production
NODE_ENV=development
